html
    head
      meta(charset="UTF-8")
      link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css")

    body(style = "padding: 20px;")
      div(class="container-fluid")
      
        h1(class="header")
          | Image Search Abstraction Layer
        blockquote
          p User stories:
          ul
            li 
              p I can get the image URLs, alt text and page urls for a set of images relating to a given search string.
              p 
                b Example URL: 
                code https://guarded-meadow-55288.herokuapp.com/imagesearch/tacos
            br
            li
              p I can paginate through the responses by adding a ?offset=2 parameter to the URL.
              p
                b Example URL:
                code https://guarded-meadow-55288.herokuapp.com/imagesearch/tacos?offset=100
            br
            li 
              p I can get a list of the most recently submitted search strings.
              p
                b Example URL:
                code https://guarded-meadow-55288.herokuapp.com/latest